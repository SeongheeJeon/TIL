1. String

2. Boolean

3. Number

4. Array
  - JS에서의 배열은 다른 타입의 값들로도 구성 가능.
  - 배열의 값으로 변수도 가능.

5. Object

# Array와 Object의 차이
  -> array - 값들의 리스트일뿐. 이름x. 대괄호[]사용
  -> object - 각 값들이 이름을 갖고있음. 중괄호{} 사용

# const로 선언된 object의 경우, 해당 object자체의 값을 바꿀 수는 없는데,
  object에 속해있는 특정 키의 값은 바꿀 수 있음.

